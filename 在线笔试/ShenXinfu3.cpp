#include <iostream>
#include <cstdio>
#include <algorithm>

using namespace std;

int main(){
    //freopen("1.in","r",stdin);
    int n,ans = 1000000000,sum=100000000;
    cin >> n;
    for(int i = 0; i < n; i++){
        int x; 
        scanf("%d",&x);
        scanf("%d",&x);
        scanf("%d",&x);
        scanf("%d",&x);
        scanf("%d",&x);
        scanf("%d",&x);
        cout << "UNKNOWN" << endl;
    }
    
    return 0;
}